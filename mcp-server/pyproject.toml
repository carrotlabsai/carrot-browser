[project]
name = "carrot-mcp-server"
version = "0.1.0"
description = "MCP server for controlling Chrome via the Carrot Bridge extension"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.26.0",
]

[project.scripts]
carrot-mcp = "server:main"
